# V2.4 Quality Appraisal Checklist

Status: V2.4 item-level quality appraisal closeout verified on the local quality-appraisal feature branch.

## Scope

- Keep `literature-screening-v2.2/` as the compatibility path.
- Keep the V2.3 PRISMA-trAIce audit export trio frozen.
- Do not connect a real AI provider by default.
- Do not collect, save, or export API keys.
- Keep quality judgement human-controlled; the app may create template rows and baselines, but not final quality ratings.

## Implemented

| Item | Status |
|---|---|
| Quality appraisal schema version | `quality_appraisal.v2.4-alpha` |
| Template version | `v2.4-alpha` |
| Template families | RCT, cohort, case-control, cross-sectional, diagnostic accuracy, systematic review |
| Tool families | RoB 2, ROBINS-I, Newcastle-Ottawa Scale, JBI, QUADAS-2, AMSTAR 2 |
| Judgement options | `low_risk`, `some_concerns`, `high_risk`, `unclear`, `not_applicable`, `not_assessed` |
| Export file | `quality_appraisal.csv` |
| Evidence table export | `evidence_table.csv` |
| GRADE summary export | `grade_summary.csv` |
| Reviewer-editable item-level forms | Step 5 domain judgement, supporting quote, reviewer note, overall judgement, status, and assessment notes |
| Item-level audit trace | `quality_appraisal_updated` with before/after snapshots and `metadata.editor = item_level_quality_form` |
| Audit event | `quality_export_generated` |
| Evidence export audit event | `evidence_table_export_generated` |
| GRADE export audit event | `grade_summary_export_generated` |
| Local-file UX hardening | language fallback CSS, upload click fallback, built-in sample-data fallback |

## Export Boundary

V2.4 adds `quality_appraisal.csv`, `evidence_table.csv`, and `grade_summary.csv` as quality deliverables. They are intentionally outside the frozen V2.3 audit package trio and do not change these V2.3 files:

- `project_manifest.json`
- `events.jsonl`
- `screening_decisions.csv`
- `exclusion_reasons.csv`
- `prisma_counts.json`
- `audit_summary.md`
- `ai_usage_registry.json`
- `ai_suggestions.jsonl`
- `PRISMA_TRAICE_REPORT.md`

## Verification

Focused checks:

```powershell
node --check literature-screening-v2.2\app.js
node --check literature-screening-v2.2\quality-engine.js
node --test tests\quality\evidence-engine.test.mjs tests\quality\study-design-classifier.test.mjs tests\audit\audit-export.test.mjs tests\audit\audit-workflow.test.mjs tests\audit\audit-engine.test.mjs
```

Full regression:

```powershell
node tests\run-all-regressions.js
```

Latest verified checkpoint on 2026-05-11:

- 115 tests passed after the item-level quality form closeout.
- 0 tests failed.
- Syntax checks passed for `literature-screening-v2.2\app.js` and `literature-screening-v2.2\quality-engine.js`.
- Browser automation was attempted for the in-app browser, but the browser connection timed out in this environment; repeat a manual Step 5 UI smoke check before tagging a public release.

## Handoff

V2.4 can close after the item-level form slice passes focused checks and full regression.

V2.5 should then focus on dual-review and conflict workflow formalization:

- reviewer isolation
- quality-review conflict queue
- resolver workflow
- agreement metrics
- final export gate for unresolved conflicts

Keep final certainty, GRADE summary, and downgrade reasons human-controlled.
